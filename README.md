# Sign Language CodeLearn

Sign-language recognition project with preprocessing notebooks, model training, and an application entry point.

## Dataset and trained model
The CSV datasets and trained model are stored separately. Add the Google Drive dataset link here after upload.
